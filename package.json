{
  "name": "gearflow",
  "version": "1.0.0",
  "private": true,
  "description": "GearFlow cycling gear asset management mini SaaS",
  "scripts": {
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run prisma:generate --prefix server && npm run build --prefix client",
    "start": "npm run start --prefix server",
    "db:migrate": "npm run prisma:migrate --prefix server",
    "db:push": "npm run prisma:push --prefix server",
    "db:seed": "npm run seed --prefix server",
    "ui:smoke": "node scripts/ui-smoke.cjs"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "playwright": "^1.61.1"
  },
  "engines": {
    "node": ">=20"
  }
}
